#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/sttr/static.stable.yaml
_disabled: false

pkg: "sttr"
pkg_id: "github.com.abhimanyu003.sttr"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "abhimanyu003/sttr"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/abhimanyu003/sttr"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/abhimanyu003/sttr/main/LICENSE"
note:
  - "Official binary from https://github.com/abhimanyu003/sttr"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "sttr"
src_url:
  - "https://github.com/abhimanyu003/sttr"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/abhimanyu003/sttr/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/abhimanyu003/sttr@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o sttr.tar.gz ||     soar dl "https://github.com/abhimanyu003/sttr@v${PKGVER}" --glob "*${ARCH}*" --yes -o sttr.tar.gz ||     soar dl "https://github.com/abhimanyu003/sttr@v${PKGVER}" --glob "*linux*" --yes -o sttr.tar.gz
    tar -xf "sttr.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "sttr*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/sttr" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/sttr" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "sttr.tar.gz"
