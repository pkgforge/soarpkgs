#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/taplo/static.stable.yaml
_disabled: false

pkg: "taplo"
pkg_id: "github.com.tamasfe.taplo"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "tamasfe/taplo"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/tamasfe/taplo"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/tamasfe/taplo/main/LICENSE"
note:
  - "Official binary from https://github.com/tamasfe/taplo"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "taplo"
src_url:
  - "https://github.com/tamasfe/taplo"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/tamasfe/taplo/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/tamasfe/taplo@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o taplo.tar.gz ||     soar dl "https://github.com/tamasfe/taplo@v${PKGVER}" --glob "*${ARCH}*" --yes -o taplo.tar.gz ||     soar dl "https://github.com/tamasfe/taplo@v${PKGVER}" --glob "*linux*" --yes -o taplo.tar.gz
    tar -xf "taplo.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "taplo*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/taplo" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/taplo" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "taplo.tar.gz"
