#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/sysbox/static.stable.yaml
_disabled: false

pkg: "sysbox"
pkg_id: "github.com.nestybox.sysbox"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "nestybox/sysbox"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/nestybox/sysbox"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/nestybox/sysbox/main/LICENSE"
note:
  - "Official binary from https://github.com/nestybox/sysbox"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "sysbox"
src_url:
  - "https://github.com/nestybox/sysbox"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/nestybox/sysbox/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/nestybox/sysbox@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o sysbox.tar.gz ||     soar dl "https://github.com/nestybox/sysbox@v${PKGVER}" --glob "*${ARCH}*" --yes -o sysbox.tar.gz ||     soar dl "https://github.com/nestybox/sysbox@v${PKGVER}" --glob "*linux*" --yes -o sysbox.tar.gz
    tar -xf "sysbox.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "sysbox*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/sysbox" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/sysbox" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "sysbox.tar.gz"
