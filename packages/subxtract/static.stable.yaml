#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/subxtract/static.stable.yaml
_disabled: false

pkg: "subxtract"
pkg_id: "github.com.pkgforge-security.subxtract"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "pkgforge-security/subxtract"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/pkgforge-security/subxtract"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/pkgforge-security/subxtract/main/LICENSE"
note:
  - "Official binary from https://github.com/pkgforge-security/subxtract"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "subxtract"
src_url:
  - "https://github.com/pkgforge-security/subxtract"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/pkgforge-security/subxtract/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/pkgforge-security/subxtract@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o subxtract.tar.gz ||     soar dl "https://github.com/pkgforge-security/subxtract@v${PKGVER}" --glob "*${ARCH}*" --yes -o subxtract.tar.gz ||     soar dl "https://github.com/pkgforge-security/subxtract@v${PKGVER}" --glob "*linux*" --yes -o subxtract.tar.gz
    tar -xf "subxtract.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "subxtract*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/subxtract" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/subxtract" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "subxtract.tar.gz"
