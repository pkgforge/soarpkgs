#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/tcpdump/static.stable.yaml
_disabled: false

pkg: "tcpdump"
pkg_id: "github.com.the-tcpdump-group.tcpdump"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "the-tcpdump-group/tcpdump"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/the-tcpdump-group/tcpdump"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/the-tcpdump-group/tcpdump/main/LICENSE"
note:
  - "Official binary from https://github.com/the-tcpdump-group/tcpdump"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "tcpdump"
src_url:
  - "https://github.com/the-tcpdump-group/tcpdump"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/the-tcpdump-group/tcpdump/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/the-tcpdump-group/tcpdump@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o tcpdump.tar.gz ||     soar dl "https://github.com/the-tcpdump-group/tcpdump@v${PKGVER}" --glob "*${ARCH}*" --yes -o tcpdump.tar.gz ||     soar dl "https://github.com/the-tcpdump-group/tcpdump@v${PKGVER}" --glob "*linux*" --yes -o tcpdump.tar.gz
    tar -xf "tcpdump.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "tcpdump*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/tcpdump" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/tcpdump" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "tcpdump.tar.gz"
