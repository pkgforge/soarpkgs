#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/subs/static.stable.yaml
_disabled: false

pkg: "subs"
pkg_id: "github.com.0x4f53.subs"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "0x4f53/subs"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/0x4f53/subs"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/0x4f53/subs/main/LICENSE"
note:
  - "Official binary from https://github.com/0x4f53/subs"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "subs"
src_url:
  - "https://github.com/0x4f53/subs"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/0x4f53/subs/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/0x4f53/subs@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o subs.tar.gz ||     soar dl "https://github.com/0x4f53/subs@v${PKGVER}" --glob "*${ARCH}*" --yes -o subs.tar.gz ||     soar dl "https://github.com/0x4f53/subs@v${PKGVER}" --glob "*linux*" --yes -o subs.tar.gz
    tar -xf "subs.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "subs*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/subs" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/subs" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "subs.tar.gz"
