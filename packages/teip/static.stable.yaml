#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/teip/static.stable.yaml
_disabled: false

pkg: "teip"
pkg_id: "github.com.greymd.teip"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "greymd/teip"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/greymd/teip"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/greymd/teip/main/LICENSE"
note:
  - "Official binary from https://github.com/greymd/teip"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "teip"
src_url:
  - "https://github.com/greymd/teip"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/greymd/teip/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/greymd/teip@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o teip.tar.gz ||     soar dl "https://github.com/greymd/teip@v${PKGVER}" --glob "*${ARCH}*" --yes -o teip.tar.gz ||     soar dl "https://github.com/greymd/teip@v${PKGVER}" --glob "*linux*" --yes -o teip.tar.gz
    tar -xf "teip.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "teip*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/teip" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/teip" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "teip.tar.gz"
