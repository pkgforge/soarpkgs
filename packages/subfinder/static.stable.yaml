#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/subfinder/static.stable.yaml
_disabled: false

pkg: "subfinder"
pkg_id: "github.com.projectdiscovery.subfinder"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "projectdiscovery/subfinder"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/projectdiscovery/subfinder"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/projectdiscovery/subfinder/main/LICENSE"
note:
  - "Official binary from https://github.com/projectdiscovery/subfinder"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "subfinder"
src_url:
  - "https://github.com/projectdiscovery/subfinder"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/projectdiscovery/subfinder/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/projectdiscovery/subfinder@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o subfinder.tar.gz ||     soar dl "https://github.com/projectdiscovery/subfinder@v${PKGVER}" --glob "*${ARCH}*" --yes -o subfinder.tar.gz ||     soar dl "https://github.com/projectdiscovery/subfinder@v${PKGVER}" --glob "*linux*" --yes -o subfinder.tar.gz
    tar -xf "subfinder.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "subfinder*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/subfinder" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/subfinder" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "subfinder.tar.gz"
