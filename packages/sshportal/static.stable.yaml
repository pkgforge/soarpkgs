#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/sshportal/static.stable.yaml
_disabled: false

pkg: "sshportal"
pkg_id: "github.com.moul.sshportal"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "moul/sshportal"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/moul/sshportal"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/moul/sshportal/main/LICENSE"
note:
  - "Official binary from https://github.com/moul/sshportal"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "sshportal"
src_url:
  - "https://github.com/moul/sshportal"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/moul/sshportal/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/moul/sshportal@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o sshportal.tar.gz ||     soar dl "https://github.com/moul/sshportal@v${PKGVER}" --glob "*${ARCH}*" --yes -o sshportal.tar.gz ||     soar dl "https://github.com/moul/sshportal@v${PKGVER}" --glob "*linux*" --yes -o sshportal.tar.gz
    tar -xf "sshportal.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "sshportal*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/sshportal" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/sshportal" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "sshportal.tar.gz"
