#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/packages/kanon/static.stable.yaml
_disabled: false

pkg: "kanon"
pkg_id: "gpjp-hades/kanon-app"
pkg_type: "static"
ghcr_pkg: "gpjp-hades/kanon-app"
category:
  - "Utility"
description: "null"
homepage:
  - "https://github.com/gpjp-hades/kanon-app"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: ""MIT"
    url: "https://github.com/gpjp-hades/kanon-app/blob/main/LICENSE"
note:
  - "Official binary from https://github.com/gpjp-hades/kanon-app"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "kanon"
repology:
  - "kanon"
src_url:
  - "https://github.com/gpjp-hades/kanon-app"
tag:
  - utility
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/gpjp-hades/kanon-app/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    case "$(uname -m)" in
      aarch64)
        soar dl "https://github.com/gpjp-hades/kanon-app" --match "appimage" --exclude "x86,x64,arm,zsync" -o "./${PKG}" --yes
        ;;
      x86_64)
        soar dl "https://github.com/gpjp-hades/kanon-app" --match "appimage" --exclude "aarch64,arm,zsync" -o "./${PKG}" --yes
        ;;
    esac
    mv "${PKG}" "$SBUILD_OUTDIR/"
