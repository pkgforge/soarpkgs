#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/packages/arduino-cli/static.stable.yaml
_disabled: false

pkg: "arduino-cli"
pkg_id: "arduino.arduino-cli"
pkg_type: "static"
pkgver: "1.4.1"
ghcr_pkg: "arduino/arduino-cli"
category:
  - "Utility"
  - "Development"
description: "Arduino command line interface"
homepage:
  - "https://github.com/arduino/arduino-cli"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "GPL-3.0"
    url: "https://raw.githubusercontent.com/arduino/arduino-cli/master/LICENSE.txt"
note:
  - "Official binary from https://github.com/arduino/arduino-cli"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "arduino-cli"
repology:
  - "arduino-cli"
src_url:
  - "https://github.com/arduino/arduino-cli"
tag:
  - "arduino"
  - "embedded"
  - "development"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/arduino/arduino-cli/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="64bit" ;;
      aarch64) ARCH="ARM64" ;;
    esac
    soar dl "https://github.com/arduino/arduino-cli@v${PKGVER}" --glob "*Linux_${ARCH}.tar.gz" --yes -o arduino-cli.tar.gz
    tar -xf "arduino-cli.tar.gz" -C "$SBUILD_TMPDIR/"
    mv "$SBUILD_TMPDIR/arduino-cli" "$SBUILD_OUTDIR/$PKG"
    rm -rf "arduino-cli.tar.gz"
