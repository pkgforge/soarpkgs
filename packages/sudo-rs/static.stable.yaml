#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/sudo-rs/static.stable.yaml
_disabled: false

pkg: "sudo-rs"
pkg_id: "github.com.memorysafety.sudo-rs"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "memorysafety/sudo-rs"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/memorysafety/sudo-rs"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/memorysafety/sudo-rs/main/LICENSE"
note:
  - "Official binary from https://github.com/memorysafety/sudo-rs"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "sudo-rs"
src_url:
  - "https://github.com/memorysafety/sudo-rs"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/memorysafety/sudo-rs/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/memorysafety/sudo-rs@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o sudo-rs.tar.gz ||     soar dl "https://github.com/memorysafety/sudo-rs@v${PKGVER}" --glob "*${ARCH}*" --yes -o sudo-rs.tar.gz ||     soar dl "https://github.com/memorysafety/sudo-rs@v${PKGVER}" --glob "*linux*" --yes -o sudo-rs.tar.gz
    tar -xf "sudo-rs.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "sudo-rs*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/sudo-rs" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/sudo-rs" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "sudo-rs.tar.gz"
