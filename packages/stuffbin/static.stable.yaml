#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/stuffbin/static.stable.yaml
_disabled: false

pkg: "stuffbin"
pkg_id: "github.com.knadh.stuffbin"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "knadh/stuffbin"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/knadh/stuffbin"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/knadh/stuffbin/main/LICENSE"
note:
  - "Official binary from https://github.com/knadh/stuffbin"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "stuffbin"
src_url:
  - "https://github.com/knadh/stuffbin"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/knadh/stuffbin/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/knadh/stuffbin@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o stuffbin.tar.gz ||     soar dl "https://github.com/knadh/stuffbin@v${PKGVER}" --glob "*${ARCH}*" --yes -o stuffbin.tar.gz ||     soar dl "https://github.com/knadh/stuffbin@v${PKGVER}" --glob "*linux*" --yes -o stuffbin.tar.gz
    tar -xf "stuffbin.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "stuffbin*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/stuffbin" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/stuffbin" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "stuffbin.tar.gz"
