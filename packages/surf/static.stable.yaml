#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/surf/static.stable.yaml
_disabled: false

pkg: "surf"
pkg_id: "github.com.assetnote.surf"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "assetnote/surf"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/assetnote/surf"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/assetnote/surf/main/LICENSE"
note:
  - "Official binary from https://github.com/assetnote/surf"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "surf"
src_url:
  - "https://github.com/assetnote/surf"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/assetnote/surf/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/assetnote/surf@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o surf.tar.gz ||     soar dl "https://github.com/assetnote/surf@v${PKGVER}" --glob "*${ARCH}*" --yes -o surf.tar.gz ||     soar dl "https://github.com/assetnote/surf@v${PKGVER}" --glob "*linux*" --yes -o surf.tar.gz
    tar -xf "surf.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "surf*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/surf" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/surf" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "surf.tar.gz"
