#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/sunbeam/static.stable.yaml
_disabled: false

pkg: "sunbeam"
pkg_id: "github.com.pomdtr.sunbeam"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "pomdtr/sunbeam"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/pomdtr/sunbeam"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/pomdtr/sunbeam/main/LICENSE"
note:
  - "Official binary from https://github.com/pomdtr/sunbeam"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "sunbeam"
src_url:
  - "https://github.com/pomdtr/sunbeam"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/pomdtr/sunbeam/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/pomdtr/sunbeam@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o sunbeam.tar.gz ||     soar dl "https://github.com/pomdtr/sunbeam@v${PKGVER}" --glob "*${ARCH}*" --yes -o sunbeam.tar.gz ||     soar dl "https://github.com/pomdtr/sunbeam@v${PKGVER}" --glob "*linux*" --yes -o sunbeam.tar.gz
    tar -xf "sunbeam.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "sunbeam*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/sunbeam" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/sunbeam" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "sunbeam.tar.gz"
