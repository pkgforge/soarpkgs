#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/sugarfree/static.stable.yaml
_disabled: false

pkg: "sugarfree"
pkg_id: "github.com.nickvourd.SugarFree"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "nickvourd/SugarFree"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/nickvourd/SugarFree"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/nickvourd/SugarFree/main/LICENSE"
note:
  - "Official binary from https://github.com/nickvourd/SugarFree"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "sugarfree"
src_url:
  - "https://github.com/nickvourd/SugarFree"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/nickvourd/SugarFree/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/nickvourd/SugarFree@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o sugarfree.tar.gz ||     soar dl "https://github.com/nickvourd/SugarFree@v${PKGVER}" --glob "*${ARCH}*" --yes -o sugarfree.tar.gz ||     soar dl "https://github.com/nickvourd/SugarFree@v${PKGVER}" --glob "*linux*" --yes -o sugarfree.tar.gz
    tar -xf "sugarfree.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "sugarfree*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/sugarfree" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/SugarFree" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "sugarfree.tar.gz"
