#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/stree/static.stable.yaml
_disabled: false

pkg: "stree"
pkg_id: "github.com.orangekame3.stree"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "orangekame3/stree"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/orangekame3/stree"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/orangekame3/stree/main/LICENSE"
note:
  - "Official binary from https://github.com/orangekame3/stree"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "stree"
src_url:
  - "https://github.com/orangekame3/stree"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/orangekame3/stree/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/orangekame3/stree@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o stree.tar.gz ||     soar dl "https://github.com/orangekame3/stree@v${PKGVER}" --glob "*${ARCH}*" --yes -o stree.tar.gz ||     soar dl "https://github.com/orangekame3/stree@v${PKGVER}" --glob "*linux*" --yes -o stree.tar.gz
    tar -xf "stree.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "stree*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/stree" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/stree" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "stree.tar.gz"
