#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/tailscale/static.stable.yaml
_disabled: false

pkg: "tailscale"
pkg_id: "github.com.tailscale.tailscale"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "tailscale/tailscale"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/tailscale/tailscale"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/tailscale/tailscale/main/LICENSE"
note:
  - "Official binary from https://github.com/tailscale/tailscale"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "tailscale"
src_url:
  - "https://github.com/tailscale/tailscale"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/tailscale/tailscale/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/tailscale/tailscale@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o tailscale.tar.gz ||     soar dl "https://github.com/tailscale/tailscale@v${PKGVER}" --glob "*${ARCH}*" --yes -o tailscale.tar.gz ||     soar dl "https://github.com/tailscale/tailscale@v${PKGVER}" --glob "*linux*" --yes -o tailscale.tar.gz
    tar -xf "tailscale.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "tailscale*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/tailscale" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/tailscale" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "tailscale.tar.gz"
