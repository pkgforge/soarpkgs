#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/syncat/static.stable.yaml
_disabled: false

pkg: "syncat"
pkg_id: "github.com.foxfriends.syncat"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "foxfriends/syncat"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/foxfriends/syncat"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/foxfriends/syncat/main/LICENSE"
note:
  - "Official binary from https://github.com/foxfriends/syncat"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "syncat"
src_url:
  - "https://github.com/foxfriends/syncat"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/foxfriends/syncat/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/foxfriends/syncat@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o syncat.tar.gz ||     soar dl "https://github.com/foxfriends/syncat@v${PKGVER}" --glob "*${ARCH}*" --yes -o syncat.tar.gz ||     soar dl "https://github.com/foxfriends/syncat@v${PKGVER}" --glob "*linux*" --yes -o syncat.tar.gz
    tar -xf "syncat.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "syncat*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/syncat" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/syncat" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "syncat.tar.gz"
