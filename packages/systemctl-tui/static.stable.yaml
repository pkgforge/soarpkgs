#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/systemctl-tui/static.stable.yaml
_disabled: false

pkg: "systemctl-tui"
pkg_id: "github.com.rgwood.systemctl-tui"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "rgwood/systemctl-tui"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/rgwood/systemctl-tui"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/rgwood/systemctl-tui/main/LICENSE"
note:
  - "Official binary from https://github.com/rgwood/systemctl-tui"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "systemctl-tui"
src_url:
  - "https://github.com/rgwood/systemctl-tui"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/rgwood/systemctl-tui/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/rgwood/systemctl-tui@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o systemctl-tui.tar.gz ||     soar dl "https://github.com/rgwood/systemctl-tui@v${PKGVER}" --glob "*${ARCH}*" --yes -o systemctl-tui.tar.gz ||     soar dl "https://github.com/rgwood/systemctl-tui@v${PKGVER}" --glob "*linux*" --yes -o systemctl-tui.tar.gz
    tar -xf "systemctl-tui.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "systemctl-tui*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/systemctl-tui" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/systemctl-tui" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "systemctl-tui.tar.gz"
