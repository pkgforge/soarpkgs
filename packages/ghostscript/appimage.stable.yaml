#!/SBUILD
_disabled: false

pkg: "ghostscript"
pkg_id: "pkgforge-dev.ghostscript"
pkg_type: "appimage"
ghcr_pkg: "pkgforge-dev/ghostscript"
category:
  - "TODO"
description: "Unofficial AppImage of Ghostscript [Maintainer=@Samueru-sama]"
homepage:
  - "https://github.com/pkgforge-dev/Ghostscript-AppImage"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - "MIT"
build_asset:
  - url: "TODO"
    out: "LICENSE"
note:
  - "Fetched from Pre Built Community Created AppImage. Check/Report @ https://github.com/pkgforge-dev/Ghostscript-AppImage"
  - "[PORTABLE] (Works on AnyLinux)"
provides:
  - "ghostscript"
repology:
  - "ghostscript"
src_url:
  - "https://github.com/pkgforge-dev/Ghostscript-AppImage"
tag:
  - "TODO"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    VERSION=$(curl -qfsSL "https://api.github.com/repos/pkgforge-dev/Ghostscript-AppImage/releases/latest?per_page=20" | jq -r '.tag_name')
    echo "${VERSION%%@*}"
    echo "$VERSION"
  run: |
    soar dl "https://github.com/pkgforge-dev/Ghostscript-AppImage@${REMOTE_PKGVER}" --glob "*${ARCH}*.appimage" -o "./${PKG}" --yes
