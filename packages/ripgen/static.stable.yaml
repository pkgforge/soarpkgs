#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/ripgen/static.stable.yaml
# deps: docker
_disabled: false

pkg: "ripgen"
pkg_id: "resyncgg.ripgen"
pkgver: "0.1.0"
pkg_type: "static"
ghcr_pkg: "resyncgg/ripgen"
category:
  - "Development"
  - "Utility"
description: "High performance domain permutation generator"
homepage:
  - "https://github.com/resyncgg/ripgen"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "MIT"
    url: "https://github.com/resyncgg/ripgen/raw/master/LICENSE"
note:
  - "Built from source with static musl linking"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "ripgen"
repology:
  - "ripgen"
src_url:
  - "https://github.com/resyncgg/ripgen"
tag:
  - "cli"
  - "utility"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/resyncgg/ripgen/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    docker run --rm --privileged --net="host" \
      --pull="always" \
      -e "PKGVER=$PKGVER" \
      --volume "$SBUILD_TMPDIR:/output:rw" \
      "ghcr.io/pkgforge/devscripts/alpine-builder:$(uname -m)" \
      bash -c '
        set -e
        cd "$(mktemp -d)"
        source "$HOME/.cargo/env"
        export RUST_TARGET="$(uname -m)-unknown-linux-musl"
        export RUSTFLAGS="-C target-feature=+crt-static -C link-self-contained=yes"
        git clone --filter=blob:none --quiet "https://github.com/resyncgg/ripgen"
        cd ripgen
        git checkout "$PKGVER"
        cargo build --target="$RUST_TARGET" --profile=release-lto
        cp "target/$RUST_TARGET/release-lto/ripgen" /output/ripgen 2>/dev/null || cp "target/$RUST_TARGET/release/ripgen" /output/ripgen
        cp LICENSE* /output/ 2>/dev/null || true
      '
    cp "$SBUILD_TMPDIR/ripgen" "$SBUILD_OUTDIR/"
    cp "$SBUILD_TMPDIR/LICENSE"* "$SBUILD_OUTDIR/" 2>/dev/null || true
