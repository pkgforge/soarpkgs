#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/packages/clipcat/static.stable.yaml
_disabled: false

pkg: "clipcat"
pkg_id: "xrelkd.clipcat"
pkg_type: "static"
pkgver: "0.24.0"
ghcr_pkg: "xrelkd/clipcat"
category:
  - "Utility"
  - "System"
description: "A clipboard manager in Rust"
homepage:
  - "https://github.com/xrelkd/clipcat"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "GPL-3.0"
    url: "https://raw.githubusercontent.com/xrelkd/clipcat/main/LICENSE"
note:
  - "Official binary from https://github.com/xrelkd/clipcat"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "clipcat-menu"
  - "clipcat-notify"
  - "clipcatctl"
  - "clipcatd"
repology:
  - "clipcat"
src_url:
  - "https://github.com/xrelkd/clipcat"
tag:
  - "clipboard"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/xrelkd/clipcat/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    soar dl "https://github.com/xrelkd/clipcat@v${PKGVER}" --glob "*$(uname -m)-unknown-linux-musl.tar.gz" --yes -o clipcat.tar.gz
    tar -xf "clipcat.tar.gz" -C "$SBUILD_TMPDIR/"
    mv "$SBUILD_TMPDIR/clipcat-menu" "$SBUILD_OUTDIR/clipcat-menu"
    mv "$SBUILD_TMPDIR/clipcat-notify" "$SBUILD_OUTDIR/clipcat-notify"
    mv "$SBUILD_TMPDIR/clipcatctl" "$SBUILD_OUTDIR/clipcatctl"
    mv "$SBUILD_TMPDIR/clipcatd" "$SBUILD_OUTDIR/clipcatd"
    rm -rf "clipcat.tar.gz"
