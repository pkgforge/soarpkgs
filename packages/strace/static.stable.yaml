#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/strace/static.stable.yaml
_disabled: false

pkg: "strace"
pkg_id: "github.com.strace.strace"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "strace/strace"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/strace/strace"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/strace/strace/main/LICENSE"
note:
  - "Official binary from https://github.com/strace/strace"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "strace"
src_url:
  - "https://github.com/strace/strace"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/strace/strace/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/strace/strace@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o strace.tar.gz ||     soar dl "https://github.com/strace/strace@v${PKGVER}" --glob "*${ARCH}*" --yes -o strace.tar.gz ||     soar dl "https://github.com/strace/strace@v${PKGVER}" --glob "*linux*" --yes -o strace.tar.gz
    tar -xf "strace.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "strace*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/strace" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/strace" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "strace.tar.gz"
