#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/rga/static.stable.yaml
# deps: docker
_disabled: false

pkg: "rga"
pkg_id: "phiresky.ripgrep-all"
pkgver: "0.1.0"
pkg_type: "static"
ghcr_pkg: "phiresky/ripgrep-all"
category:
  - "Development"
  - "Utility"
description: "Ripgrep for PDFs, E-Books, Office docs"
homepage:
  - "https://github.com/phiresky/ripgrep-all"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "AGPL-3.0"
    url: "https://github.com/phiresky/ripgrep-all/raw/master/LICENSE"
note:
  - "Built from source with static musl linking"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "rga"
repology:
  - "rga"
src_url:
  - "https://github.com/phiresky/ripgrep-all"
tag:
  - "cli"
  - "utility"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/phiresky/ripgrep-all/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    docker run --rm --privileged --net="host" \
      --pull="always" \
      -e "PKGVER=$PKGVER" \
      --volume "$SBUILD_TMPDIR:/output:rw" \
      "ghcr.io/pkgforge/devscripts/alpine-builder:$(uname -m)" \
      bash -c '
        set -e
        cd "$(mktemp -d)"
        source "$HOME/.cargo/env"
        export RUST_TARGET="$(uname -m)-unknown-linux-musl"
        export RUSTFLAGS="-C target-feature=+crt-static -C link-self-contained=yes"
        git clone --filter=blob:none --quiet "https://github.com/phiresky/ripgrep-all"
        cd rga
        git checkout "$PKGVER"
        cargo build --target="$RUST_TARGET" --profile=release-lto
        cp "target/$RUST_TARGET/release-lto/rga" /output/rga 2>/dev/null || cp "target/$RUST_TARGET/release/rga" /output/rga
        cp LICENSE* /output/ 2>/dev/null || true
      '
    cp "$SBUILD_TMPDIR/rga" "$SBUILD_OUTDIR/"
    cp "$SBUILD_TMPDIR/LICENSE"* "$SBUILD_OUTDIR/" 2>/dev/null || true
