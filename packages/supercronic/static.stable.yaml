#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/supercronic/static.stable.yaml
_disabled: false

pkg: "supercronic"
pkg_id: "github.com.aptible.supercronic"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "aptible/supercronic"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/aptible/supercronic"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/aptible/supercronic/main/LICENSE"
note:
  - "Official binary from https://github.com/aptible/supercronic"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "supercronic"
src_url:
  - "https://github.com/aptible/supercronic"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/aptible/supercronic/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/aptible/supercronic@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o supercronic.tar.gz ||     soar dl "https://github.com/aptible/supercronic@v${PKGVER}" --glob "*${ARCH}*" --yes -o supercronic.tar.gz ||     soar dl "https://github.com/aptible/supercronic@v${PKGVER}" --glob "*linux*" --yes -o supercronic.tar.gz
    tar -xf "supercronic.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "supercronic*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/supercronic" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/supercronic" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "supercronic.tar.gz"
