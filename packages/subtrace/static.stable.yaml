#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/subtrace/static.stable.yaml
_disabled: false

pkg: "subtrace"
pkg_id: "github.com.subtrace.subtrace"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "subtrace/subtrace"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/subtrace/subtrace"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/subtrace/subtrace/main/LICENSE"
note:
  - "Official binary from https://github.com/subtrace/subtrace"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "subtrace"
src_url:
  - "https://github.com/subtrace/subtrace"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/subtrace/subtrace/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/subtrace/subtrace@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o subtrace.tar.gz ||     soar dl "https://github.com/subtrace/subtrace@v${PKGVER}" --glob "*${ARCH}*" --yes -o subtrace.tar.gz ||     soar dl "https://github.com/subtrace/subtrace@v${PKGVER}" --glob "*linux*" --yes -o subtrace.tar.gz
    tar -xf "subtrace.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "subtrace*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/subtrace" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/subtrace" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "subtrace.tar.gz"
