#!/SBUILD ver @v1.0.0
#SELF: https://raw.githubusercontent.com/pkgforge/soarpkgs/refs/heads/main/binaries/stew/static.stable.yaml
_disabled: false

pkg: "stew"
pkg_id: "github.com.marwanhawari.stew"
pkgver: "0.0.0"
pkg_type: "static"
ghcr_pkg: "marwanhawari/stew"
category:
  - "Utility"
description: "Ported from pkgs/binaries"
homepage:
  - "https://github.com/marwanhawari/stew"
maintainer:
  - "QaidVoid (contact@qaidvoid.dev)"
license:
  - id: "UNKNOWN"
    url: "https://raw.githubusercontent.com/marwanhawari/stew/main/LICENSE"
note:
  - "Official binary from https://github.com/marwanhawari/stew"
  - "[PORTABLE] (Portable Static Binary)"
provides:
  - "stew"
src_url:
  - "https://github.com/marwanhawari/stew"
x_exec:
  host:
    - "x86_64-linux"
    - "aarch64-linux"
  shell: "sh"
  pkgver: |
    curl -qfsSL "https://api.github.com/repos/marwanhawari/stew/releases/latest" | jq -r '.tag_name' | tr -d 'v'
  run: |
    ARCH=$(uname -m)
    case "$ARCH" in
      x86_64) ARCH="x86_64" ;;
      aarch64) ARCH="arm64" ;;
    esac
    soar dl "https://github.com/marwanhawari/stew@v${PKGVER}" --glob "*Linux_${ARCH}*" --yes -o stew.tar.gz ||     soar dl "https://github.com/marwanhawari/stew@v${PKGVER}" --glob "*${ARCH}*" --yes -o stew.tar.gz ||     soar dl "https://github.com/marwanhawari/stew@v${PKGVER}" --glob "*linux*" --yes -o stew.tar.gz
    tar -xf "stew.tar.gz" -C "$SBUILD_TMPDIR/" || true
    find "$SBUILD_TMPDIR/" -type f -executable -name "stew*" -exec mv {} "$SBUILD_OUTDIR/" \; ||     mv "$SBUILD_TMPDIR/stew" "$SBUILD_OUTDIR/" 2>/dev/null ||     mv "$SBUILD_TMPDIR/stew" "$SBUILD_OUTDIR/" 2>/dev/null
    rm -rf "stew.tar.gz"
